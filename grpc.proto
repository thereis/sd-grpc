syntax = "proto3";

package SD.Project;

message Chunk {
    repeated double numbers = 1;
}

message Result {
    repeated double orderedNumbers = 1;
}

service SendChunk {
    rpc sendChunk(stream Chunk) returns (Result);
}